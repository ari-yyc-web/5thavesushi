# Fifth Avenue Sushi — website

Static marketing site for Fifth Avenue Sushi (Japanese restaurant, downtown Calgary).
Plain HTML/CSS/JS, no build framework. Deployed to **Cloudflare Workers assets**
(`wrangler.toml`, `_headers`). Domain: https://www.fifthavenuesushi.ca/

## Structure

- `index.html` — homepage. Every other page lives in its own folder as `index.html`
  (`/menus/`, `/menus/<category>/`, `/our-chef/`, `/our-story/`, `/private-dining/`,
  `/large-groups-corporate/`, `/gallery/`, `/location-hours/`, `/faq/`, `/blog/`,
  `/reservations/`).
- `styles.css` — single global stylesheet. `script.js` — single global script. Both
  shared by every page (root pages link `styles.css`; nested pages use `../` / `../../`).
- Header nav, mobile menu, and footer are **hand-duplicated** into every page. Change one →
  change all. Bulk edits across footers are easiest via a small Node script.
- All images are **local WebP** (as of 2026-08-01). `img/menu/**` — dish photos (see
  provenance note below). `img/photos/**` — hero/gallery/section photos, downloaded from
  Unsplash and stored locally as WebP (filename = `<photo-id>-w<width>.webp`). Only the
  favicons/app-icons remain PNG/ICO/SVG (required formats). Nothing loads remote images.

## Menu pages are generated — do not hand-edit

The `<!-- MENU:START -->…<!-- MENU:END -->` block in each `/menus/<category>/index.html`
is generated by `build-menus.js` from `menu-data.js`. Edit `menu-data.js` (and the
`build-menus.js` category config) then re-run `node build-menus.js`. Hand-edits inside the
block get overwritten. `menu-data.js` is also the source of the Menu JSON-LD.

## Reservations & forms

- **Reservations use OpenTable**, not a form. `/reservations/` has a `.reserve-book-card`
  linking to `https://www.opentable.com/r/fifth-avenue-sushi-calgary` + a call button.
- **Inquiry forms** (currently only `/private-dining/`) use the `data-async-submit` handler
  in `script.js`: real POST to the form's `action`, success shown **only** on HTTP 200,
  `.form-error` (call/email fallback) otherwise. It never fakes success. Do not reintroduce
  the old `data-mock-submit` pattern, which silently dropped submissions.
  - ⚠️ **`private-dining/index.html` still has `action="https://formspree.io/f/YOUR_FORM_ID"`.**
    Replace with a real Formspree form ID before launch or the form won't send.

## SEO / indexing

- Indexed: home, `/menus/`, `/reservations/`, `/gallery/`, `/location-hours/`, `/faq/`.
- **`noindex`**: all `/menus/<category>/` pages, `/our-chef/`, `/our-story/`,
  `/private-dining/`, `/large-groups-corporate/`, `/blog/` — intentional, "content pending
  confirmation" (see the comment in `sitemap.xml`). Remove the `noindex` tag AND add the URL
  to `sitemap.xml` together when real content is signed off.
- `_headers` sets `Content-Type` per HTML route explicitly (a `/*` wildcard would break
  css/js/images) — add a new route here whenever you add a new page.
- **AI search / Bing:** `llms.txt` (root, markdown summary for LLMs — keep NAP/hours in
  sync with the site), `robots.txt` explicitly welcomes AI crawlers (no Disallow rules),
  homepage Restaurant JSON-LD carries `geo`/`hasMap`, and geo meta tags live on `/` and
  `/location-hours/`. IndexNow key file is `072c38759714658a1a6f6e08ca24393b.txt` (root);
  run `node indexnow-submit.js` after each deploy to ping Bing (which also feeds ChatGPT
  search). When you add/rename an indexed page, update `sitemap.xml` then re-run that script.
- **Canonical host:** `worker.js` (wired via `main` + `binding=ASSETS` + `run_worker_first`
  in `wrangler.toml`) 301-redirects non-www→www and http→https, then serves assets. For the
  apex redirect to fire, BOTH `fifthavenuesushi.ca` and `www.` must be attached to the Worker
  as custom domains in Cloudflare. This fixes the Seobility "Server config 0%" duplicate-host
  finding — but only after deploy.

## Known pre-launch gaps (owner actions, not code)

1. Formspree ID (above).
2. Photo rights: whole site is stock/AI imagery; the `img/menu` photos are platform
   "reference only" AI-extended stock — see `img/menu/README.md`. Rights unconfirmed.
3. `noindex` pages need content/price sign-off (Ed/owner) before indexing.
4. Confirm the OpenTable listing and DoorDash store (id 40338429) are really the
   restaurant's. The live Wix site does **not** use OpenTable (phone + Wix popup); the new
   site standardizes on OpenTable by choice.

## Local preview

`.claude/launch.json` runs `python -m http.server 8735`. Note: that dev server sends no
cache headers, so the browser aggressively caches `styles.css`/`script.js`/pages — hard-
reload or fetch with `cache:'no-store'` when verifying edits. Production (Cloudflare) is
unaffected.
